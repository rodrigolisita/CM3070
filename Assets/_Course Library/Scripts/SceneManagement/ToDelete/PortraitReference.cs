using UnityEngine;
using UnityEngine.UI;

// Attach this script to the specific Image GameObject used for the portrait.
public class PortraitReference : MonoBehaviour
{
    // This script is intentionally empty.
    // Its only purpose is to act as a unique identifier for GetComponentInChildren.
}